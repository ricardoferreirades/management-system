import { SortingUserPipe } from './sorting-user.pipe';

describe('SortingUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingUserPipe();
    expect(pipe).toBeTruthy();
  });
});
